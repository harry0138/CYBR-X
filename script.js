const boot=document.querySelector(".boot"),bar=document.querySelector(".progress i"),bootText=document.getElementById("bootText");setTimeout(()=>bar.style.width="100%",100);setTimeout(()=>bootText.textContent="NEURAL BUS CONNECTED / ACCESS GRANTED",900);setTimeout(()=>boot.classList.add("hidden"),1600);
const glow=document.querySelector(".cursor-glow");window.addEventListener("pointermove",e=>{glow.style.left=e.clientX+"px";glow.style.top=e.clientY+"px"});
document.querySelectorAll(".module").forEach(card=>card.addEventListener("mouseenter",()=>{document.querySelectorAll(".module").forEach(x=>x.classList.remove("active"));card.classList.add("active")}));
const counters=document.querySelectorAll("[data-count]");const io=new IntersectionObserver(entries=>{if(!entries[0].isIntersecting)return;counters.forEach(el=>{if(el.dataset.done)return;el.dataset.done=1;let target=parseFloat(el.dataset.count),step=target/35,n=0,t=setInterval(()=>{n=Math.min(target,n+step);el.textContent=Number.isInteger(target)?Math.round(n):n.toFixed(1);if(n>=target)clearInterval(t)},25)});io.disconnect()},{threshold:.35});if(counters.length)io.observe(counters[0]);
const menu=document.querySelector(".menu"),nav=document.querySelector(".nav nav");menu?.addEventListener("click",()=>{nav.style.display=nav.style.display==="flex"?"none":"flex";nav.style.flexDirection="column";nav.style.position="absolute";nav.style.top="68px";nav.style.left="0";nav.style.right="0";nav.style.padding="22px";nav.style.background="#080b0d";nav.style.borderBottom="1px solid #202a30"});
document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener("click",()=>{if(innerWidth<=650)nav.style.display="none"}));
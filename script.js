const $=s=>document.querySelector(s), $$=s=>document.querySelectorAll(s);
// boot sequence
const bootlog=['loading kernel modules...','mounting /secure/vault...','initializing threat engine...','checking encrypted channels...','loading operator profile...','session integrity: <b>100%</b>','access level: <b>GUEST / LEARNING</b>'];let bl=$('#bootlog');bootlog.forEach((x,i)=>setTimeout(()=>{let d=document.createElement('div');d.innerHTML='[ '+String(i+1).padStart(2,'0')+' ] '+x;bl.appendChild(d)},i*250));setTimeout(()=>$('#boot').classList.add('hide'),2600);
// clock + session
let start=Date.now();function tick(){let d=new Date();$('#clock').textContent=d.toLocaleTimeString('en-GB');let s=Math.floor((Date.now()-start)/1000);$('#session').textContent=String(Math.floor(s/60)).padStart(2,'0')+':'+String(s%60).padStart(2,'0')}setInterval(tick,1000);tick();
// matrix rain
const c=$('#matrix'),ctx=c.getContext('2d');let W,H,cols,drops;function resize(){W=c.width=innerWidth;H=c.height=innerHeight;cols=Math.floor(W/14);drops=Array(cols).fill(1)}resize();addEventListener('resize',resize);function matrix(){ctx.fillStyle='rgba(3,6,4,.075)';ctx.fillRect(0,0,W,H);ctx.fillStyle='#65ff72';ctx.font='12px Fira Code';for(let i=0;i<drops.length;i++){let ch='01ABCDEF$#@%&'[Math.floor(Math.random()*16)];ctx.fillText(ch,i*14,drops[i]*14);if(drops[i]*14>H&&Math.random()>.975)drops[i]=0;drops[i]++}requestAnimationFrame(matrix)}matrix();
// cursor
const dot=$('.cursor-dot'),ring=$('.cursor-ring');let mx=0,my=0,rx=0,ry=0;addEventListener('pointermove',e=>{mx=e.clientX;my=e.clientY;dot.style.left=mx+'px';dot.style.top=my+'px'});function cursor(){rx+=(mx-rx)*.15;ry+=(my-ry)*.15;ring.style.left=rx+'px';ring.style.top=ry+'px';requestAnimationFrame(cursor)}cursor();
// hero terminal
const lines=[['$ whoami','guest@cybr-x'],['$ uname -a','SECURE-LAB / CYBR-X / 2026'],['$ scan --mode defensive','ports: filtered · services: monitored'],['$ cat mission.txt','learn → build → test → defend'],['$ status','ALL SYSTEMS NOMINAL']];let tl=$('#terminalLines');let li=0;function terminalLoop(){let [a,b]=lines[li%lines.length],d=document.createElement('div');d.innerHTML='<b>'+a+'</b><br>&nbsp;&nbsp;'+b;tl.appendChild(d);if(tl.children.length>5)tl.removeChild(tl.firstChild);li++;setTimeout(terminalLoop,950)}setTimeout(terminalLoop,1200);
// reveal
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.14});$$('.reveal').forEach(x=>io.observe(x));
// animated counter
let count=0;setInterval(()=>{$('#threatCount').textContent=String(count++%10000).padStart(4,'0')},75);
// radar
const rc=$('#radar'),rctx=rc.getContext('2d');function radar(){let w=rc.width,h=rc.height,cx=w/2,cy=h/2,R=120,vals=[.86,.72,.78,.68,.62,.58],n=6;rctx.clearRect(0,0,w,h);rctx.strokeStyle='rgba(101,255,114,.12)';rctx.lineWidth=1;for(let k=1;k<=4;k++){rctx.beginPath();for(let i=0;i<n;i++){let a=-Math.PI/2+i*Math.PI*2/n,rr=R*k/4;rctx.lineTo(cx+Math.cos(a)*rr,cy+Math.sin(a)*rr)}rctx.closePath();rctx.stroke()}for(let i=0;i<n;i++){let a=-Math.PI/2+i*Math.PI*2/n;rctx.beginPath();rctx.moveTo(cx,cy);rctx.lineTo(cx+Math.cos(a)*R,cy+Math.sin(a)*R);rctx.stroke()}rctx.beginPath();vals.forEach((v,i)=>{let a=-Math.PI/2+i*Math.PI*2/n,rr=R*v;let x=cx+Math.cos(a)*rr,y=cy+Math.sin(a)*rr;i?rctx.lineTo(x,y):rctx.moveTo(x,y)});rctx.closePath();rctx.fillStyle='rgba(101,255,114,.09)';rctx.fill();rctx.strokeStyle='#65ff72';rctx.stroke();vals.forEach((v,i)=>{let a=-Math.PI/2+i*Math.PI*2/n,rr=R*v;rctx.fillStyle='#65ff72';rctx.beginPath();rctx.arc(cx+Math.cos(a)*rr,cy+Math.sin(a)*rr,3,0,7);rctx.fill()})}radar();
// console
const log=$('#consoleLog');const messages=[['[+]','packet watcher online','log-ok'],['[*]','rotating session token',''],['[+]','integrity check passed','log-ok'],['[?]','no active threat signatures','log-warn'],['[*]','waiting for operator input','']];let mi=0;function consoleTick(){let m=messages[mi++%messages.length],d=document.createElement('div');d.className=m[2];d.textContent=m[0]+' '+new Date().toLocaleTimeString('en-GB')+' '+m[1];log.appendChild(d);if(log.children.length>9)log.removeChild(log.firstChild)}setInterval(consoleTick,900);for(let i=0;i<6;i++)consoleTick();
$('#consoleForm').addEventListener('submit',e=>{e.preventDefault();let v=$('#cmd').value.trim().toLowerCase();let out=v==='help'?'commands: whoami · skills · status · clear':v==='whoami'?'guest / cybersecurity explorer / ethical learning mode':v==='skills'?'linux · python · networking · web security · forensics':v==='status'?'SYSTEM ONLINE / NO ACTIVE THREATS':v==='clear'?'':`command not found: ${v}`;if(v==='clear'){log.innerHTML=''}else{let d=document.createElement('div');d.className=v==='status'?'log-ok':'';d.textContent='> '+out;log.appendChild(d)}$('#cmd').value='';log.scrollTop=log.scrollHeight});
// modal dossiers
const data={network:['OP-01 // NETWORK SENTINEL','Network visibility concept. Capture traffic in a controlled lab, classify protocols, flag anomalies and visualize evidence. Replace this dossier with your real project link, screenshots and measured results.'],web:['OP-02 // WEB SECURITY LAB','A safe learning environment for web application security. Focus on threat modeling, secure input handling, authentication and responsible testing. Add your own lab notes and proof here.'],ctf:['OP-03 // CTF // RED ROOM','A challenge track for practicing enumeration, cryptography, forensics and defensive thinking. Add your CTF writeups, ranks and certificates only when verified.']};$$('.inspect').forEach(b=>b.addEventListener('click',()=>{let d=data[b.dataset.modal];$('#modalCode').textContent=d[0];$('#modalTitle').textContent=d[0].split('// ')[1];$('#modalBody').textContent=d[1];$('#modal').classList.add('open')}));$('#closeModal').onclick=()=>$('#modal').classList.remove('open');$('#modal').addEventListener('click',e=>{if(e.target.id==='modal')$('#modal').classList.remove('open')});
// active nav + keyboard easter egg
const sections=$$('section[id]'),links=$$('nav a');const sio=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)links.forEach(a=>a.style.color=a.getAttribute('href')==='#'+e.target.id?'#65ff72':'')}),{threshold:.5});sections.forEach(s=>sio.observe(s));
let seq=[];addEventListener('keydown',e=>{if(['INPUT','TEXTAREA'].includes(document.activeElement.tagName))return;seq.push(e.key.toLowerCase());seq=seq.slice(-8);if(seq.join(',')==='a,r,r,o,w, ,h,a,c,k'){document.body.classList.toggle('overdrive')}});

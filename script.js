const loader=document.querySelector('.loader');window.addEventListener('load',()=>setTimeout(()=>loader.classList.add('loaded'),1650));
const cursor=document.querySelector('.cursor');window.addEventListener('pointermove',e=>{cursor.style.left=e.clientX+'px';cursor.style.top=e.clientY+'px';});
document.querySelectorAll('a').forEach(a=>{a.addEventListener('mouseenter',()=>{cursor.style.width='34px';cursor.style.height='34px'});a.addEventListener('mouseleave',()=>{cursor.style.width='18px';cursor.style.height='18px'})});
const layers=[...document.querySelectorAll('.layer')];let ticking=false;function parallax(){const y=window.scrollY;layers.forEach(el=>{const speed=parseFloat(el.dataset.speed||0);const rect=el.closest('.section, .scene')?.getBoundingClientRect();const center=rect?rect.top+rect.height/2:0;const delta=(window.innerHeight/2-center);el.style.transform=`translate3d(0,${delta*speed}px,0)`});ticking=false}window.addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(parallax);ticking=true}},{passive:true});parallax();
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.section-inner,.mission,.timeline article,.skills div,.terminal-big').forEach(el=>{el.classList.add('reveal');observer.observe(el)});
const lines=['$ boot --secure','> firewall ........ ONLINE','> intrusion monitor . ACTIVE','> identity .......... VERIFIED','> session ........... ENCRYPTED','> welcome, operator_'];const out=document.getElementById('termLines');let i=0;function typeLine(){if(i>=lines.length)return;const p=document.createElement('div');p.textContent=lines[i++];out.appendChild(p);setTimeout(typeLine,260)}setTimeout(typeLine,1900);

const scene=document.getElementById('scene');const vector=document.getElementById('vector');let tx=0,ty=0,x=0,y=0;function setPointer(e){const r=scene.getBoundingClientRect();tx=Math.max(-1,Math.min(1,(e.clientX-r.left)/r.width-.5)*2);ty=Math.max(-1,Math.min(1,(e.clientY-r.top)/r.height-.5)*2)}scene.addEventListener('pointermove',setPointer,{passive:true});scene.addEventListener('pointerleave',()=>{tx=0;ty=0});function animate(){x+=(tx-x)*.08;y+=(ty-y)*.08;scene.style.setProperty('--x',x.toFixed(4));scene.style.setProperty('--y',y.toFixed(4));vector.textContent=`X ${String(Math.round((x+1)*50)).padStart(2,'0')} / Y ${String(Math.round((y+1)*50)).padStart(2,'0')}`;requestAnimationFrame(animate)}animate();
